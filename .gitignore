node_modules/
dist/
*.log
.DS_Store
# Local scratch test project used by the smoke test
.smoke-tmp/
# HTTP+Postgres server runtime pid file
.http-pg.pid
# Per-user agent memory — never commit (may contain unrelated project context)
.claude/agent-memory/
# Playwright MCP scratch output (snapshots, screenshots, console logs)
.playwright-mcp/
overview-counts.png
# Local environment (credentials, workspace path) — never commit
.env
