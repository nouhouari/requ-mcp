# Host-built artifacts must never overwrite the image's own (native modules are
# platform-specific: better-sqlite3 built on macOS breaks in the Linux image).
node_modules/
dist/
.git/
.smoke-tmp/
.playwright-mcp/
*.log
.DS_Store
