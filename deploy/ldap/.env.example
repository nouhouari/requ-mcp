# Copy to deploy/ldap/.env (git-ignored) and set the two passwords.
LDAP_ORGANISATION=Example
LDAP_DOMAIN=example.com
LDAP_ADMIN_PASSWORD=change-me            # cn=admin,dc=example,dc=com
LDAP_SERVICE_USERNAME=requ-service
LDAP_SERVICE_PASSWORD=change-me          # cn=requ-service,dc=example,dc=com — REQU_LDAP_BIND_PASSWORD
LDAP_PORT=3389
LDAPS_PORT=3636
LDAP_ADMIN_PORT=8389                     # phpLDAPadmin
