# Rendered by Ansible (requ deployment) — do not edit on the host.
# Single-quoted so filters, DNs and separators pass through untouched.
{% for key, value in requ_env_map.items() %}
{% if value is not none and (value | string | length) > 0 %}
{{ key }}='{{ value }}'
{% endif %}
{% endfor %}
