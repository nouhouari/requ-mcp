[defaults]
inventory = ./inventory
roles_path = ./roles
retry_files_enabled = False
interpreter_python = auto_silent
forks = 10
# Secrets are rendered with no_log; keep the rest readable.
stdout_callback = default
result_format = yaml

[ssh_connection]
pipelining = True
